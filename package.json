{
  "name": "fl-blockchain-reward",
  "version": "1.0.0",
  "description": "FL-Blockchain reward distribution with multi-dimensional contribution",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "fund": "hardhat run scripts/fund_accounts.js --network localhost",
    "setup": "npm run compile && npm run deploy && npm run fund && npm run healthcheck",
    "healthcheck": "hardhat run scripts/healthcheck.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0"
  }
}